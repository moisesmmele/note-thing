services:
  database:
    image: mongo:latest
    container_name: note-thing-mongo
    volumes:
      - mongo_data:/data/db
    environment:
      MONGODB_DATABASE: ${MONGO_DATABASE}
      MONGODB_USER: ${MONGO_USER}
      MONGODB_PASSWORD: ${MONGO_PASSWORD}
    networks:
      - app_net
  queue:
    image: redis:latest
    container_name: note-thing-queue
    networks:
      - app_net

volumes:
  mongo_data:
    name: ${MONGO_VOLUME_NAME}
    external: true

networks:
  app_net:
    name: ${NETWORK_NAME}
    external: true

include:
  - services/sync/docker-compose.yml
  - services/ai-processor/docker-compose.yml
  - services/interaction/docker-compose.yml
  - services/delivery/docker-compose.yml
